rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인 여부 상관없이 모든 읽기 허용 (테스트 후 반드시 원복!)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}